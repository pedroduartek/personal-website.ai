{
  "name": "personal-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4"
  },
  "pnpm": {
    "overrides": {
      "react": "19.2.4",
      "react-dom": "19.2.4"
        ,
        "html-encoding-sniffer": "5.0.0",
        "jsdom": "27.4.0"
        ,
        "@exodus/bytes": "1.11.0"
    }
  }
}
